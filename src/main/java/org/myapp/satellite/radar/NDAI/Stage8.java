package org.myapp.satellite.radar.NDAI;

import java.io.File;
import java.io.FileInputStream;
import java.io.ObjectInputStream;

public class Stage8 {

    public static void main(String[] args) {
        //ObjectInputStream ois = new ObjectInputStream(new FileInputStream(stablePointIndexesDir + File.separator + bandName + ".dat"));
        //int[] ia = (int[]) (ois.readObject());
    }
}
